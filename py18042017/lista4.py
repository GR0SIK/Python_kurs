lista = ["kwiatek", "woda", "doniczka", "ziemia"]

el_to_emove = "kwiatek"

lista.remove(el_to_emove)
print(lista)

#
# lista.remove(el_to_emove)

if el_to_emove in lista:
    lista.remove(el_to_emove)

    